# Third-party material

The MIT license at the repository root applies to original Allomer code. Third-party components retain their own licenses.

- Raster tracing uses [VTracer 0.6.5](https://crates.io/crates/vtracer/0.6.5) and [visioncortex 0.8.10](https://crates.io/crates/visioncortex/0.8.10) under MIT OR Apache-2.0. The original Rust helper accepts prepared RGBA pixels and writes SVG paths. It uses the public region API for opacity masks. `tools/visioncortex-color-sum.patch` widens color totals to prevent overflow in large regions. That patch uses the MIT license. `helpers/vectortrace/Cargo.lock` pins the resolved crates. `tools/native-sources.json` pins the patched crate's original archive and checksum. `tools/build-rust.py vectortrace` retains notices, the original crate, patch, helper and launcher sources, build inputs, and hashes. It recognizes both LICENSE and LICENCE filenames. The transitive flo_curves 0.3.1 archive declares Apache-2.0 in its manifest but has no separate license file at commit `fccd933942532562557be2a61a706ebc0a1f1888`. Its notice folder includes that declaration and the [official Apache 2.0 text](https://www.apache.org/licenses/LICENSE-2.0.txt). The matching unmodified crate is retained under `.tools/vectortrace/sources`. The app bundles the two executables and notices; image decoding comes from macOS.

- TIFF JPEG output uses [LibTIFF 4.7.2](https://libtiff.gitlab.io/libtiff/releases/v4.7.2.html) and the existing libjpeg-turbo 3.2.0 source pin. LibTIFF uses its permissive libtiff license. libjpeg-turbo retains its IJG, BSD-3-Clause, and Zlib notices. `tools/build-tiff.py` builds only the static copy command and an original MIT sandbox launcher. The original `tools/tiff-metadata.patch` and `helpers/tiff/metadata.h` retain each page's XMP, EXIF, and GPS data during compression. Source archives, the patch, original helper source, build commands, and notices stay in `.tools/tiff/`. The app bundles the tools and notices. System zlib is supplied by macOS.

- Private local research and build caches must not be included in a source release or app bundle.
- PowerPoint input uses [pptx-renderer](https://github.com/aiden0z/pptx-renderer) at commit `5b8280f3d72fcab764837b705d07f229e08bfe47` under Apache-2.0. The original native helper loads its static browser bundle into macOS WebKit. No Node.js runtime is bundled. `tools/build-presentation.py` verifies the source archive, applies `tools/presentation-rendering.patch`, and builds against the exact npm lock file. The patch selects SVG charts, reports slide and image failures, and normalizes internal media and relationship paths to Unicode NFC. The build entry includes only the parser, renderer, and media resolver needed for conversion. The native wrapper serves validated media from private files as needed. The bundle includes ECharts 6.0.0 and zrender 6.0.0, JSZip 3.10.1 under its MIT option, mtx-decompressor 1.4.2 under MPL-2.0, and their pinned dependencies. Notices include pako's MIT and zlib texts, ECharts' notices, and the renderer's third-party notices. The build retains the renderer and MTX source archives, patch, lock file, build script, hashes, and notices under `.tools/presentation`. Supply this matching source with distributions. The app bundles the browser code and notices. Optional PDF.js is not bundled. Esbuild 0.28.2 is a development compiler only. Python fixture tools are development dependencies only.
- DOCX input to PDF uses [docx-preview 0.4.0](https://github.com/VolodymyrBaydalka/docxjs/tree/0.4.0) under Apache-2.0. The official tag resolves to commit `191d3e0db009da578fbe4da70d55305cd8d50226`. Its source archive is pinned in `tools/native-sources.json` with SHA-256 `b3639cd6d40dd5a6e7ddc6ba538846e52d11cc323731ba952584d5035a4a33fe`. The build compiles its TypeScript source with the same pinned esbuild the presentation renderer uses, into one 173,100-byte bundle with SHA-256 `6a579d2071b119b14967e9b6efb63495689b87a3502a2d83cfc4c0e16b161c0b`. Its only dependency, the existing JSZip 3.10.1 pin under its MIT option, is bundled into that same file. The original native helper loads the bundle into macOS WebKit as an injected script with page JavaScript disabled. Its LICENSE is the Apache 2.0 text carrying the notice Copyright (c) 2016-2023 Volodymyr Baydalka, and the archive has no NOTICE file. The repository already keeps that standard text in [licenses/Apache-2.0.txt](licenses/Apache-2.0.txt). No Node.js runtime is bundled. Supply this matching source with distributions.
- The document adapter invokes a source build of [carta 0.0.10](https://github.com/mfkrause/carta/releases/tag/v0.0.10) as a separate process. The official tag resolves to commit `df621b1074c9751494cedc782342765cacdd81de`. Its source archive has SHA-256 `3ef3695c0d95837075408bb374e85e15a5b33e1f7be9fe91fcf90ed14ef2106f`. Carta uses MIT OR Apache-2.0. `tools/build-rust.py carta` builds the CLI and full format features without network fetching or the optional copyleft grammar pack. The retained `tools/carta-resources.patch` decodes local resource URLs, bounds local reads, and reports missing container images. The original changes use the repository's MIT license. The build collects dependency notices, both Carta licenses, and the full permissive grammar XML files with their individual notices under `.tools/carta/licenses`. It retains the matching source archive, patch, lock file, build script, and hashes under `.tools/carta`. The app bundles the notices. The prebuilt upstream executable was used only during earlier development.
- Native platform conversion uses macOS frameworks. Their implementation is supplied by macOS.
- JPEG XL output uses the official [libjxl 0.12.0](https://github.com/libjxl/libjxl/releases/tag/v0.12.0) command under BSD-3-Clause, with its patent grant. Highway uses Apache-2.0, with its additional BSD license retained. skcms uses BSD-3-Clause. Brotli 1.2.0 uses MIT, and libpng 1.6.58 uses libpng-2.0. The included APNG reader has its own retained license. macOS supplies zlib. `tools/build-jpegxl.py` reads the pinned source manifest and reuses the existing Brotli and PNG versions. It retains source archives, the original launcher and build script, hashes, and notices under `.tools/jpegxl`. The app bundles `cjxl`, its original sandbox launcher, and notices. The upstream `djxl` decoder is retained only for development checks. Optional viewers, Java bindings, JPEG/GIF input libraries, and development tools are disabled. No third-party source patch is applied. Supply the matching retained sources and notices with distributions.
- Still WebP output invokes the official [libwebp 1.6.0](https://chromium.googlesource.com/webm/libwebp/+/refs/tags/v1.6.0) `cwebp` tool through an original native launcher. The original animation writer uses the same encoder and PNG reader. It writes full frames into the public WebP container without retaining the complete encoded file. libwebp and SharpYUV use BSD-3-Clause with the upstream patent grant. PNG input uses [libpng 1.6.58](https://www.libpng.org/pub/png/libpng.html) under libpng-2.0. macOS supplies zlib. `tools/native-sources.json` pins the official source archives and checksums. `tools/build-webp.py` retains both source archives, the original writer and launcher sources, build script, manifest, hashes, and notices under `.tools/webp`. No third-party source patch is applied. The app bundles the still encoder, animation writer, their two launchers, and notices. Viewers, JPEG/TIFF/GIF input libraries, and unused command tools are disabled in this build. Original image preparation uses macOS ImageIO. Supply the matching retained source and notices with distributions.
- The original web helper uses macOS WebKit for SVG rendering and HTML printing, AppKit for dimensions and print operations, and CoreGraphics and ImageIO for output. Markdown PDF templates are original CSS. Its source and launcher use this repository's MIT license. `tools/build-native.py` retains their source hashes and build instructions. The app bundles the small helper executables. WebKit remains a system dependency. Development checks use Pillow, pypdf, and Poppler; these are not bundled. A local system font is copied only into an ignored test fixture directory.
- The original native helper uses macOS Vision for image, scanned PDF, and picture-subtitle text recognition. It uses CoreText and CoreGraphics for PDF text layers, ImageIO for image input, and PDFKit for text checks. Its original subtitle bridge links the existing LGPL FFmpeg libraries in `Contents/Frameworks/Media`. No second codec copy is bundled. Its implementation and launcher use this repository's MIT license. `tools/build-native.py` retains their source hashes, linked-library hashes, and build instructions. No OCR model weights or third-party OCR engine are copied into the app; macOS supplies the frameworks and recognition models. Matching FFmpeg source and notices remain required for distribution, as described below.
- Level 3 PostScript and EPS output uses [Poppler 26.09.0](https://poppler.freedesktop.org/) under GPL-2.0-or-later. `tools/build-pdftops.py` retains matching sources, build commands, resource hashes, and two original source patches under `.tools/poppler`. The patches use GPL-2.0-or-later. They load local resources, propagate conversion errors, and emit native gradients for exponential and stitched color functions. FreeType 2.14.3 uses the FreeType License. libjpeg-turbo 3.2.0 retains its IJG, BSD, and zlib notices. Little CMS 2.19.1 uses MIT. OpenJPEG 2.5.4 uses BSD-2-Clause. The poppler-data 0.4.12 character maps retain their Adobe and GPL-2.0/GPL-3.0 notices. Fourteen font files come from the pinned Ghostscript archive under AGPL-3.0-or-later, with its document embedding exception. The command links only to macOS dynamic libraries. The app bundles it, its original MIT sandbox launcher, fonts, character maps, and notices under `Contents/Resources/Licenses/poppler`. Portions of this software are copyright © 2026 The FreeType Project (https://freetype.org). All rights reserved. This software is based in part on the work of the Independent JPEG Group. Supply the matching sources, patches, and notices with a release.
- The original `helpers/pdf-overlay/main.c` command adds an existing text layer to a private PDF copy. This source uses the repository's MIT license. `tools/mupdf-overlay.patch` registers it in MuPDF and uses AGPL-3.0-or-later. The combined executable uses AGPL-3.0-or-later. The retained source bundle includes both patches, the original command source, and its MIT license. The DOCX patch also exports invisible text, including OCR layers. Independent OCR checks use development-only pypdfium2/PDFium; they are not bundled in the app.
- The original `helpers/jpeg-check/main.c` command checks JPEG data with the IJG library already linked into MuPDF. It uses MuPDF's allocator interface to bound JPEG allocations. The source uses the repository's MIT license; its registration patch and the combined executable use AGPL-3.0-or-later. The source builder retains the matching command source and notices. The app does not bundle another JPEG library or helper executable. The independent fixture check uses a development-only libjpeg-turbo `cjpeg` encoder.
- The PDF adapter invokes [MuPDF 1.28.3](https://github.com/ArtifexSoftware/mupdf/releases/tag/1.28.3) through the original native launcher. The official source archive has SHA-256 `37c3209dc0e06fa4f3781ed44839ad933a9e6143eb4731f99e069204715bcef2`. MuPDF and its Extract document writer use AGPL-3.0-or-later. The changes in `tools/mupdf-docx.patch` use the same license. They fix image decoding, transforms, ownership, page collection, and required DOCX image dimensions. `tools/build-mupdf.py` retains the matching source archive, patch, launcher source, build inputs, and notices under `.tools/mupdf`. The app bundles the notices under `Contents/Resources/Licenses/mupdf`. The static build includes upstream third-party engines and fallback fonts with their individual notices. Portions of this software are copyright © 2026 The FreeType Project (https://freetype.org). All rights reserved. This software is based in part on the work of the Independent JPEG Group. JavaScript, network fetching, GUI tools, and Tesseract are disabled. Both executables link only to macOS libraries. Supply the complete matching source bundle with a distributed release.
- The PDF/PostScript adapter invokes [Ghostscript 10.07.1](https://github.com/ArtifexSoftware/ghostpdl-downloads/releases/tag/gs10071) as a separate process through an original native launcher. The official source archive has SHA-256 `2fc74362f9be6fae1b0a65d38fdcfd4f0b518cc3b07c5581fb661eb4d2e15251`. Ghostscript uses AGPL-3.0-or-later. Its built-in fonts retain the document-embedding exception in the upstream LICENSE. Adobe character maps retain their own notices. `tools/build-postscript.py` preserves source archives, launcher source, rebuild instructions, and notices, including font and character-map headers, FreeType's license files, and the Independent JPEG Group's README. Portions of this software are copyright © 2025 The FreeType Project (https://freetype.org). All rights reserved. This software is based in part on the work of the Independent JPEG Group. The source archive does not include the incompatible optional JPEG XR directory. Both executables link only to macOS libraries. The app bundles notices under `Contents/Resources/Licenses/pdf`. Supply the complete matching source bundle with a distributed release. Independent PDF checks use development-only reportlab, pypdf, Pillow, and Poppler; those check tools are not bundled.
- The original model helper links a static [Assimp 6.0.5](https://github.com/assimp/assimp/tree/v6.0.5) build under BSD-3-Clause. Its official source archive has SHA-256 `edf3749559c2b7d1f758ffb66fc5bec62186221e623b7f2e8969f17ee46ecb6f`. The PLY writer has a small, retained patch that fixes its UV and color declarations. `tools/build-models.py` builds only the needed readers and writers and retains the patch hash. Assimp includes Draco 1.5.7 under Apache-2.0 and other components listed in `.tools/models/licenses`. The app bundles these notices. The upstream test models and RapidJSON JSON_checker test code are not part of the app. Native USD reading and USDZ writing use macOS ModelIO and SceneKit. Independent FBX checks use [ufbx 0.23.0](https://github.com/ufbx/ufbx/tree/v0.23.0), under its MIT option, from checksum-pinned files in a development-only cache. Its code and executable are not bundled. All model fixtures in the checks are original.
- The original email helper uses [mail-parser 0.11.8](https://github.com/stalwartlabs/mail-parser), [mail-builder 0.5.0](https://github.com/stalwartlabs/mail-builder), and [ammonia 4.1.4](https://github.com/rust-ammonia/ammonia) under MIT OR Apache-2.0. It uses [cfb 0.14.0](https://github.com/mdsteele/rust-cfb) and [compressed-rtf 1.0.1](https://github.com/microsoft/outlook-pst-rs) under MIT. `helpers/mailfile/Cargo.lock` pins all dependencies and registry checksums. `tools/build-rust.py mailfile` retains dependency notices, including files inside upstream `LICENSES` directories. The compressed-rtf crate omits its root license file. Its [MIT notice](licenses/compressed-rtf-1.0.1.txt) comes from upstream commit `1397836e73b690dbb09663f66056012fced45ff9`, as recorded in the crate's VCS metadata. The transitive cssparser 0.37.0 and dtoa-short 0.3.5 crates use MPL-2.0. Their unmodified crate source archives are copied from the verified Cargo cache to `.tools/mailfile/sources`, with the lock-file hashes and source manifest. Supply that matching source bundle with a distributed release. The helper links only to macOS libraries. The original MSG mapping follows Microsoft's public file specification. The RTF bridge calls AppKit. Independent checks use extract-msg 0.56.1 and its dependencies in a development-only virtual environment; none are bundled.
- The font helper is original code that links pinned static libraries. [HarfBuzz 14.4.0](https://github.com/harfbuzz/harfbuzz/releases/tag/14.4.0) supplies the subsetter that rewrites a font at its default design location, removes variation tables, converts CFF2 to CFF, and drops the signature. Its official source archive has SHA-256 `2357ed966c6ced7bfa720b0640c0231065af01158fbea215093ffa15aed44371`, and it uses the Old MIT license. Its shapers, rasterizers, and platform integrations are disabled; only the subsetter is built. `hb-subset.h` includes `hb-subset-depend.h`, which HarfBuzz 14.4.0 omits from its CMake install list, so `tools/build-fonts.py` copies that released header into the build prefix. No upstream source is changed. Google's WOFF2 at commit `fb9c3379f2605b10f3e8f1d9636664ab5576775c` and [Brotli 1.2.0](https://github.com/google/brotli/releases/tag/v1.2.0) provide the WOFF2 codec, both under MIT. The helper reads outlines and writes TrueType glyph data with the fontations libraries [read-fonts 0.44.0](https://github.com/googlefonts/fontations), [skrifa 0.47.0](https://github.com/googlefonts/fontations), and [write-fonts 0.53.0](https://github.com/googlefonts/fontations), with [kurbo 0.13.1](https://github.com/linebender/kurbo) for curve conversion and [miniz_oxide 0.9.1](https://github.com/Frommi/miniz_oxide) for the WOFF 1.0 container, all under MIT OR Apache-2.0 or a superset. `helpers/fontconvert/Cargo.lock` pins every crate version and registry checksum, and `tools/native-sources.json` pins the C and C++ sources and hashes. `tools/build-rust.py fontconvert` retains dependency notices, the resolved dependency manifest, the lock file, Rust 1.98.1's standard library notices, and the helper's own sources under `.tools/fontconvert`. `tools/build-fonts.py` collects upstream license and author files under `.tools/fonts/licenses` and retains source archives and a rebuild script under `.tools/fonts/sources`. The app bundles both sets of notices. A distributed release must provide the complete matching source bundle. The CFF writer, the WOFF 1.0 reader and writer, and the sandbox launcher are original. The helper and its launcher link only to libraries supplied by macOS. The small font fixtures contain original test shapes, names, and metrics; they use the repository's MIT license. Their generator needs a separately installed FontForge, which is not built, linked, or shipped. Independent font checks use development-only fontTools 4.62.1 and Brotli 1.2.0, and the Adobe Variable Font Prototype 1.004 fixtures under the SIL Open Font License, in an ignored cache; none of those are bundled.
- The ebook adapter invokes [libmobi 0.12](https://github.com/bfabiszewski/libmobi/releases/tag/v0.12) as a separate process. Its official source archive has SHA-256 `9a6fb2c56b916f8fa8b15e0c71008d908109508c944ea1d297881d4e277bf7e7`. `tools/build-ebook.py` builds the unmodified upstream reader with encryption disabled. libmobi uses LGPL-3.0-or-later. Its embedded miniz 1.15 uses the Unlicense. macOS supplies libxml2 and zlib. The build retains the matching source archive, rebuild script, command log, LGPL and GPL texts, and miniz notice under `.tools/ebook/`. The app bundles the notices under `Contents/Resources/Licenses/ebook`. A distributed release must provide the matching source bundle. Upstream ebook fixtures are used from the ignored build directory for checks; they are not original application code or bundled sample books.
- The original spreadsheet helper uses [calamine 0.36.1](https://github.com/tafia/calamine/tree/v0.36.1) under MIT, [rust_xlsxwriter 0.99.0](https://github.com/jmcnamara/rust_xlsxwriter/tree/v0.99.0) under MIT OR Apache-2.0, and [csv 1.4.0](https://github.com/BurntSushi/rust-csv) under MIT or Unlicense. `helpers/tabular/Cargo.lock` pins all crate versions and registry checksums. `tools/build-rust.py tabular` copies upstream license and notice files for resolved dependencies, records their declared licenses, and preserves Rust 1.98.1's standard library notices. Those notices are bundled under `Contents/Resources/Licenses/tabular`. The app contains the ARM64 helper, not the Rust compiler. The optional fixture generator downloads xlwt 1.3.0 under BSD with wheel SHA-256 `a082260524678ba48a297d922cc385f58278b8aa68741596a87de01a9c628b2e`. Its generated workbook contains original test values. The generator's dependency is not part of the app or source release.
- The archive adapter links to the `libarchive.2` library supplied by macOS. The two unmodified declaration headers in `Sources/CArchive/include` come from [libarchive 3.7.4](https://github.com/libarchive/libarchive/tree/v3.7.4/libarchive). Their SHA-256 hashes are `618cd16c8fa40ddabc44f2d3eb3a35860f6b1d1d856b62a7f4f035b7a319bdab` (`archive.h`) and `3695eb0193741b16b99e06cf1fc5e6db427b68a9475696ef746c024ae0a6ae72` (`archive_entry.h`). The exact header notices and upstream license summary are retained in `licenses/libarchive-headers-BSD.txt` and `licenses/libarchive-BSD.txt`. The library implementation is supplied by macOS. Runtime behavior still needs verification on the minimum supported macOS version.
- YAML uses [Yams 6.2.2](https://github.com/jpsim/Yams/releases/tag/6.2.2), pinned at `a27b21e0c81c5bf42049b897a62aaf387e80f279` in `Package.resolved`. Yams and its vendored LibYAML use the MIT license. Their notices are in `licenses/Yams-MIT.txt` and `licenses/LibYAML-MIT.txt`. An original event reader calls LibYAML directly for JSON and YAML. It checks nesting and alias expansion while constructing configuration values. Yams supplies YAML scalar rules and output encoding.
- TOML uses the unmodified [toml++ 3.4.0](https://github.com/marzer/tomlplusplus/releases/tag/v3.4.0) amalgamated header in `Sources/ConfigBridge/vendor/toml.hpp`. Its SHA-256 is `6b5172ad4dd6519aec67b919181fa7a38a2234131e5b2afa232dfe444819783e`. The upstream download is `https://raw.githubusercontent.com/marzer/tomlplusplus/v3.4.0/toml.hpp`. Its MIT notice and the embedded UTF-8 decoder's notice are in `licenses/tomlplusplus-MIT.txt` and `licenses/UTF8-decoder-MIT.txt`. The original bridge uses C++ object lifetimes and frees its returned buffer after each call. No TOMLKit wrapper is linked or distributed.
- The media adapter invokes a bundled FFmpeg 9.0.1 source build. It reports LGPL-2.1-or-later and enables neither GPL nor nonfree components. FFmpeg and FFprobe share the bundled media libraries under `Contents/Frameworks/Media`. The other codec dependencies are linked into those libraries. Pinned sources are listed in `tools/native-sources.json`. Source archives, build commands, and upstream license files are retained under the ignored `.tools/media/` directory. LAME, Ogg, Vorbis, Opus, VPX, AOM, and SVT-AV1 retain their own notices. A release must include those notices and matching source distribution. FFmpeg documents how optional components affect its license on its [license page](https://ffmpeg.org/legal.html). Homebrew's GPL `ffmpeg-full` 9.0.1 revision 1 was used for initial development checks; the app no longer uses that binary.
- [libaom](https://aomedia.googlesource.com/aom) 3.15.0 supplies the AVIF encoder used when the host macOS ImageIO cannot write AVIF. The fallback preserves transparency as a separate AV1 alpha plane. Its BSD-2-Clause notice and checksum-pinned source archive are retained with the media build. The app keeps the native ImageIO writer on macOS versions that provide it.
- [dav1d](https://www.videolan.org/projects/dav1d.html) 1.5.4 supplies software AV1 decoding, including on Apple Silicon Macs without an AV1 hardware decoder. Its BSD-2-Clause copyright and license text and its upstream AV1 patent notice are retained in the media notices. The source archive is verified against VideoLAN's published SHA-256 checksum. The library is linked into the existing media libraries. No separate decoder command is bundled. Meson and Ninja are development tools only; the media and font builds share their catalog pins.
- `tools/ffmpeg-jpeg-validation.patch` changes FFmpeg's strict JPEG decoding to reject a missing end marker. Normal decoding keeps upstream behavior. This original patch uses LGPL-2.1-or-later, like the changed source file. Its source, hash, and change notice are retained by the media builder and must accompany the matching distribution.
- `tools/ffmpeg-gif-validation.patch` makes strict GIF decoding reject incomplete pixel rows and an incomplete block stream. The four added lines use LGPL-2.1-or-later, like the changed FFmpeg files. Default decoding behavior is unchanged. The media builder retains the patch, source, hash, and change notice. APNG frame decoding uses the existing bundled FFmpeg to avoid a native poster-frame compositing error.
- `tools/ffmpeg-gif-palette.patch` permits two colors, optionally excludes hidden pixels from palette analysis, and preserves exact colors in unsplit entries. Its optional bounded per-frame histogram retries with 6-bit color channels after 65,536 distinct colors. An optional cache limit retains the same color selection without unbounded cache growth. Frames without color reduction skip dithering. The patch uses LGPL-2.1-or-later, like the changed filters, and is retained with its hash and change notice. Original Swift code prepares sRGB RGBA pixels with ImageIO and the system Accelerate framework, then runs the bundled FFmpeg encoder. An original streaming parser restores source GIF delays. No new library or executable is bundled.
- The documentation site uses Docusaurus 3.10.2, React 19.2.8, and `@easyops-cn/docusaurus-search-local` 0.55.3 under their upstream MIT licenses. Search builds a local browser index and uses no hosted search service. Exact transitive versions are recorded in `website/package-lock.json`. These packages are not part of the native app runtime. Preserve the generated JavaScript license notices when publishing the site.

Other converters identified in the investigation are not yet dependencies. Add their source versions, notices, and build instructions when integrating them.

The checked carta tag resolves to commit `df621b1074c9751494cedc782342765cacdd81de`. Its default feature set includes the full converter, CLI, and optional network-fetch support; the adapter passes `--sandbox` to disable fetching. The separate copyleft-grammar feature is not part of the default `full` feature set. Verify the release build settings and collect transitive notices before bundling it for distribution.

The docs lock file includes patched overrides for `serialize-javascript`, `qs`, and the `sockjs` use of `uuid`. It resolves Docusaurus's compatible `image-size` dependency to 2.0.4, which contains the parser fixes required by the current npm audit. Re-run the audit and production build after every documentation dependency update.
